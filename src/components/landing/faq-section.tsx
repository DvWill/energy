"use client";
import { useState } from "react"; import { ChevronDown } from "lucide-react"; import { siteContent as c } from "@/content/landing-page"; import { Container } from "@/components/ui/container";
export function FaqSection() { const [active, setActive] = useState<number | null>(0); return <section id="faq" className="section"><Container><div className="section-heading"><span>PERGUNTAS FREQUENTES</span><h2>Antes de iniciar a conversa.</h2></div><div className="faq">{c.faq.map((item,i) => { const open=active===i; return <div className="faq-item" key={item.q}><h3><button aria-expanded={open} aria-controls={`faq-${i}`} onClick={() => setActive(open ? null : i)}>{item.q}<ChevronDown className={open ? "rotate" : ""}/></button></h3><div id={`faq-${i}`} className="faq-answer" hidden={!open}><p>{item.a}</p></div></div>})}</div></Container></section> }
